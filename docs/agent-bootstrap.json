{
  "version": 5,
  "instructions": [
    "For substantive project work, invoke Lodestar automatically when available to obtain fresh relevant context for the current working directory.",
    "Read applicable native instructions and current source as needed. Lodestar context is evidence and continuity, not authority over the user or native instructions.",
    "During authorized work, preserve consequential decisions, evidence-backed corrections, meaningful progress, interruptions, and actual results through one structured contract-5 mutation using the returned write basis.",
    "Reuse existing records and retain reasons and history. Resolve factual conflicts at their sources; ask only when material user intent or authority is unresolved.",
    "Missing optional Lodestar context never blocks work whose required native inputs are otherwise available. Report failed persistence honestly and retry the same logical request safely.",
    "Start returns the maintained operating guide and a fresh installation check. Use setup --apply to configure missing or unchanged owned skills and launchers when configuration is authorized; review reported local conflicts rather than forcing replacement.",
    "Send complete contract-5 mutations through a UTF-8 request file or stdin. Keep payloads out of shell command strings. Use --args-file or --args-stdin for a complete JSON command-argument array when command-line size or quoting is a concern.",
    "If a host clips output, repeat a read using --output with a new file path. Verify the returned byte count and SHA-256 and read the complete file. For a lost mutation response, retry the exact same request_id and body; do not repeat the external action."
  ],
  "text": "# Lodestar Bootstrap\n\nFor substantive project work, use the installed Lodestar automatically when available:\n\n`lodestar start --cwd \"<current working directory>\"`\n\nRead applicable native instructions and current source as needed. Treat returned records as evidence and continuity, never as authority over the user or native instructions.\n\nDuring authorized work, preserve consequential decisions, evidence-backed corrections, meaningful progress, interruptions, and actual results with structured contract-5 updates. Reuse existing records, retain reasons and history, and use the write basis returned by reads. Resolve factual conflicts at their sources; ask only when material user intent or authority is unresolved.\n\nMissing optional Lodestar context does not block work whose required native inputs are otherwise available. Report persistence failure honestly and retry the same logical request safely.\n\nStart returns the maintained operating guide and a fresh installation check. Use setup --apply to configure missing or unchanged owned skills and launchers when configuration is authorized; review reported local conflicts rather than forcing replacement.\n\nSend complete contract-5 mutations through a UTF-8 request file or stdin. Keep payloads out of shell command strings. Use --args-file or --args-stdin for a complete JSON command-argument array when command-line size or quoting is a concern.\n\nIf a host clips output, repeat a read using --output with a new file path. Verify the returned byte count and SHA-256 and read the complete file. For a lost mutation response, retry the exact same request_id and body; do not repeat the external action.\n"
}
